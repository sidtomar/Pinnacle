# Core LangChain
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0
# LangGraph — provides create_react_agent used by Agent Alpha (required)
langgraph>=0.2.0

# Search
tavily-python>=0.3.0
# LangChain Tavily integration — provides langchain_tavily.TavilySearch (required by tools/search.py)
langchain-tavily>=0.1.0

# Microsoft OneDrive / Graph API
msal>=1.28.0
requests>=2.31.0
python-docx>=1.1.0
PyPDF2>=3.0.0
openpyxl>=3.1.0

# WhatsApp - Twilio
twilio>=9.0.0

# Email
sendgrid>=6.11.0

# Azure Databricks — primary data store
# Connects to Databricks SQL Warehouses via the official Python driver
databricks-sql-connector>=3.0.0

# Databricks Vector Search — semantic search over MA document library
# Manages ANN index, embedding generation, and Unity Catalog governance
databricks-vectorsearch>=0.40

# LangChain × Databricks integration (embeddings + chat models via FMAPI)
langchain-databricks>=0.1.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
tenacity>=8.2.0

# Scheduling (daily doctor sync + content generation)
apscheduler>=3.10.4
