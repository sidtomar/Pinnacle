[build]
builder = "NIXPACKS"
buildCommand = "pip install -r demo/backend/requirements.txt"

[deploy]
startCommand = "python demo/backend/app.py"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
